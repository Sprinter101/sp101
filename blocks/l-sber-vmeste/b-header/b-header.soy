{namespace sv.lSberVmeste.bHeader.Template}

/**
 * @constructor
 * @extends cl.iControl.Template
 */
{template .header}
    {call .init data="all"/}
{/template}

/**
 * @override
 */
{template .body}
<div class="{call .rootClass/}__wrapper">
	<ul class="{call .rootClass/}__nav-links">
		{call .tab}
			{param params: [
				'navLinks': [
					'Header'
				]
			]/}
		{/call}
	</ul>
</div>
{/template}

/**
 * Tab template
 * @param params {{
 *     navLinks: list
 * }}
 */
{template .tab}
	{foreach $item in $params.navLinks}
		<li class="{call .rootClass/}__nav-link">{$item}</li>
	{/foreach}
{/template}

/**
 * @override
 */
{template .rootClass}
    b-header
{/template}

/**
 * @param params {
 *     config: {
 *         isHidden: ?bool
 *     }
 * }
 */
{template .additionalClasses}
    {if $params?.config?.isHidden}
        i-utils__hidden{sp}
    {/if}
{/template}